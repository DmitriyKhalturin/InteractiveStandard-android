pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "InteractiveStandard"

include(":app")
include(":data")
include(":domain")
